const getArgs = require('./helpers/args')

const startCli = ()=> {
    const args = getArgs(process.argv)
    if(args.h){
        //h -help
    }
    if (args.s){
        //s -save
    }
if (args.t) {
    // save - token 
}
// result 
}


startCli()